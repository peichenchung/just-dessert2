<h1>All users</h1>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= image_tag user.avatar.url if user.avatar? %>
    <%= user.name %>
    <%= user.email %>
    <%= user.intro %>
    <%= user.contact_info %>
    <%= user.role %>
  </li>
  <% end %>
</ul>